#include <stdio.h>
int checkPrime(int n) {
    if (n <= 1) return 0;
    for (int i = 2; i <= n / 2; ++i) {
        if (n % i == 0) return 0;
    }
    return 1;
}
int main() {
    int n;
    printf("Enter integer: ");
    scanf("%d", &n);
    if (checkPrime(n)) printf("%d is Prime\n", n);
    else printf("%d is Not Prime\n", n);
    return 0;
}
