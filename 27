#include <stdio.h>
int main() {
    int base, exp;
    long long result = 1;
    printf("Enter base and exponent: ");
    scanf("%d %d", &base, &exp);
    while (exp != 0) {
        result *= base;
        --exp;
    }
    printf("Answer = %lld\n", result);
    return 0;
}
