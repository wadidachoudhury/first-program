#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    int idx = 0;
    printf("Enter number string: ");
    scanf("%s", str);
    while (str[idx] == '0') idx++;
    if (str[idx] == '\0') printf("0\n");
    else printf("Result: %s\n", str + idx);
    return 0;
}
